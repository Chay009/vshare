import { useEffect, useContext } from 'react';
import {AuthContext} from '@/context/AuthProvider';

const useAuth = () => {

  
  // Set user initially


  // To make user persist


  return useContext(AuthContext);
};

export default useAuth;
